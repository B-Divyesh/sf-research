collection-escape-hatch — API-client switching keeps exposing importer and offline-portability failures.
internal-event-ledger — Small teams need searchable operational events without turning Slack into an alert landfill.
async-trace-stitcher — Distributed failures still require manual reconstruction across partial logs and vendor systems.
event-payload-spillway — Large event bodies can silently convert workflow history into a database disk-outage risk.
sqlite-sync-guard — File sync plus live SQLite remains a data-loss trap with a tractable preflight/export wedge.
restore-drill — Self-hosters need evidence that a restore works before an incident forces the first attempt.
secret-sync-preflight — Multi-hop config and provider limits create key-level drift that values-only secret managers do not show.
flag-removal-map — Small teams need a safe bridge from a completed rollout to actual flag-code deletion.
arazzo-proof-runner — Arazzo users still lack a simple runner that leaves a reproducible workflow evidence artifact.
collector-pressure-lab — OTel users need to test queueing and drops for their config before telemetry goes dark under load.
migration-replay-gate — Generated schema changes need repeat/partial-state safety tests, not just a clean-environment diff.
css-cause-map — Live layout bugs need deterministic, exportable explanations beyond manually toggling DevTools styles.
