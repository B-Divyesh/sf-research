local-meeting-export — local transcript-to-vault ownership is newly urgent as cloud recorder exports tighten.
gpu-vram-burnin — users explicitly want MemTest-like GPU-memory evidence, while silent stalls remain hard to classify.
apk-provenance-locker — Android sideloading friction makes a lawful, offline restoration inventory timely.
epub-annotation-bridge — readers want Calibre-compatible sideloading without sacrificing portable notes.
diagram-source-studio — diagrams-as-code still needs native, offline rendering verification before commit.
presence-bridge — small businesses keep rejecting full chat suites over the missing, uncluttered presence layer.
ios-review-gate — small teams need to make App Review queues and submission packets predictable.
installer-release-doctor — cross-platform packaging friction is still stopping CLI authors from shipping.
worktree-agent-pulse — parallel CLI-agent work has made worktree state, not generation, the bottleneck.
local-model-residency — offline LLM usability needs transparent memory/residency diagnostics, not another chat client.
manuscript-entity-indexer — multilingual authors need private story continuity without cloud uploads or giant models.
agent-cli-contract — agent-facing CLIs need reproducible interface tests before automation can be trusted.
