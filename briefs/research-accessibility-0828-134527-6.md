terminal-screenreader-mode — live terminal output still freezes or confuses screen readers; a local stable transcript layer is a clear gap.
stream-reader-compass — streamed chats lose focus and structure, making a user-controlled semantic reading surface timely.
local-live-captions — students still lack captions for meetings and recordings, especially outside Windows and major platforms.
caption-layout-keeper — platform subtitle regressions erase speaker and layout meaning, not just visual decoration.
caption-choice-memory — users repeatedly fight caption language and auto-enable defaults that ought to be personal preferences.
color-signal-lens — global filters do not fix color-only signals in high-frequency tasks such as code review.
voice-line-navigator — voice control needs addressable, trustworthy navigation targets rather than generic dictation.
project-color-beacons — wrong-window edits are a concrete dyslexia/ADHD workflow risk that stable multi-cue project identity can reduce.
read-along-queue — readers want local speech inside their existing reading flow, not another account-bound audio service.
keyboard-route-check — teams need a reviewable record of real Tab and reverse-Tab paths before component defects ship.
screenreader-task-audit — automated scans miss the interactive workflows that can block blind founders from their own products.
silent-focus-sentinel — a local focus-sequence checker can catch silent VoiceOver stops before users discover them.
