# Devtools/data opportunities — 2026-08-28

scheduled-run-receipts — missed scheduled work is invisible in ordinary logs and uptime checks.
restore-rehearsal-card — recent control-plane outage evidence makes tested recovery more urgent than possessing backups.
relational-fixture-scrubber — teams need production-shaped test data without broken foreign keys or exposed PII.
env-contract-check — parser and environment drift still turn valid-looking .env files into deployment failures.
secret-exposure-path — coding agents widen the route from a local secret to logs, artifacts, and git.
scrubbed-log-casefile — engineers need safe, correlatable incident bundles before sharing debugging evidence.
project-install-capsule — trying an unfamiliar project needs repeatable least-privilege execution, not blind Docker trust.
workspace-sandbox-manifest — high-reaction demand shows workspace-only coding-agent access remains unresolved across platforms.
repo-protocol-gate — teams need deterministic enforcement for agent-era repository rules rather than prompt instructions.
agent-kill-switch-drill — production agent operators need to rehearse withdrawing authority, not just observe failures.
agent-action-receipt — self-authored logs are inadequate proof when tools make consequential state changes.
trajectory-test-cases — CI needs deterministic assertions over critical agent tool paths rather than fragile transcript snapshots.
