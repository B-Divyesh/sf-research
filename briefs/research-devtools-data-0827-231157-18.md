function-flow-canvas — 2025–26 requests show that developers still need a narrow, language-aware way to read a real call path.
api-scenario-patch — API teams are losing affordable shared context; Git-reviewable flow patches avoid replacing their HTTP client.
agent-secret-capsule — Agent access now produces concrete secret-output leaks, creating demand for per-command scoped secret handling.
release-env-fingerprint — Environment drift is breaking real CI/deployments, while existing vaults do not verify safe configuration shape.
migration-commit-witness — Migration tooling can report success despite partial commit behavior, so independently exercised evidence is useful.
ci-outage-witness — Opaque CI degradation and runner errors make a timestamped per-run evidence packet timely.
webmcp-safety-check — Browser-agent safety properties are currently prose and defaults; protocol work is making the gap visible now.
journey-failure-receipts — A 200 response can hide a broken user flow, and Playwright users lack evidence at the precise soft-assertion failure.
restore-drill-attestor — Small teams need proof that recovery works before an outage, without replacing their backup engine.
sqlite-workload-lab — SQLite performance claims and binary compatibility need reproducible workload and CPU evidence.
log-duplicate-lens — Cross-stream retry duplicates can quietly inflate logs, alerting, and spend in otherwise capable log stacks.
api-profile-guard — Multi-environment API work needs a local, client-neutral preflight before the wrong profile reaches production.
